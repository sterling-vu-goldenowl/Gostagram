/* var error_type = "<%= escape_javascript @error_password %>"

if ( error_type == true ) {
  $('.edit-form').hide();
  $('#edit-left-menu .edit-tab').removeClass('active');
  $('#edit-left-menu .edit-tab:last-child').addClass('active');
  $('.edit-password').data('edit').show();
  //$('.' + $(this).data('edit')).show();
}
*/
